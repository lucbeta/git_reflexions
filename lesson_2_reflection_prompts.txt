What happens when you initialize a repository? Why do you need to do it?

A file .git is added to the directory, which ables git to run properly. 

How is the staging area different from the working directory and the repository? What value do you think it offers?

The staging area allows one to budle different changes on the document to a sound commmit.

How can you use the staging area to make sure you have one commit per logical change?

With the staging area one have the possibility to pick meanigful changes in the code in one commit!

What are some situations when branches would be helpful in keeping your history organized? How would branches help?

In case one want add a new feature or try someting else experimental, without the effect on the core code.

How do the diagrams help you visualize the branch structure?

One have the overview of the commits and the branches!

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

All the changes made on one branch will bin transfered to the other branch as a new commit. This new commit represents the merge of the two branches!